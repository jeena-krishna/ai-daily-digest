# This file marks the `agent` directory as a Python package.
# It allows other files to import from this directory:
#   from agent.curator import curate_digest
# It's intentionally empty — no code needs to run on import.
